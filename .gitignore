node_modules

# misc
.DS_Store
# macOS folder icon metadata
Icon?
.vscode
.claude/worktrees
*.local.json

# env files
.env
.env.*
!.env.example

# Cloud Run infra（秘密・鍵はコミットしない）
# config.env / config.env.<env>（環境別）をまとめて除外（config.example.env は対象外）
infra/cloud-run/config.env
infra/cloud-run/config.env.*
*-key.json
# Deployment metadata and local verification output
.vercel/
artifacts/
# Personal operator handover notes (kept locally, outside public source)
/docs/安城版みらい議会_*
